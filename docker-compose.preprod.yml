# Overrides for preprod environment.
# Usage: docker compose -f docker-compose.yml -f docker-compose.preprod.yml --profile preprod up -d

services:
  app-preprod:
    build:
      context: .
      dockerfile: services/app/Dockerfile
      args:
        UMAMI_APP_ID: ${UMAMI_APP_ID}
    restart: unless-stopped
    environment:
      - FLASK_ENV=development
      - SECRET_KEY=${SIDDHARTH_SECRET_KEY_PREPROD}
    profiles: ["preprod"]
    networks:
      web:
        aliases:
          - app-preprod

  streamlit-preprod:
    build:
      context: ./services/streamlit
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      - STREAMLIT_SERVER_HEADLESS=true
      - STREAMLIT_SERVER_PORT=8501
      - STREAMLIT_SERVER_ENABLE_CORS=false
      - UMAMI_HOST=${UMAMI_HOST:-https://analytics.engineerfamily.net}
      - UMAMI_STREAMLIT_ID=${UMAMI_STREAMLIT_ID:-}
    profiles: ["preprod"]
    networks:
      web:
        aliases:
          - streamlit-preprod
